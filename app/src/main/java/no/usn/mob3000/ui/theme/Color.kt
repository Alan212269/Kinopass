package no.usn.mob3000.ui.theme

import androidx.compose.ui.graphics.Color

// Grunnfarger brukt i temaet for appen.
// Disse brukes i ColorScheme / MaterialTheme for å gi appen et konsistent uttrykk.

val Purple = Color(0xFF6750A4)         // Primær lilla-farge (lys variant)
val PurpleDark = Color(0xFF4C3A88)     // Mørkere variant av lilla (f.eks. for dark theme)
val Secondary = Color(0xFF625B71)      // Sekundærfarge brukt til mindre markeringer
val BackgroundLight = Color(0xFFFCF7FF) // Lys bakgrunn for light theme
val BackgroundDark = Color(0xFF121212)  // Mørk bakgrunn for dark theme
val Accent = Color(0xFFE91E63)         // Aksentfarge (knapper/markeringsdetaljer)
val Success = Color(0xFF00C853)        // Grønn "suksess"-farge (f.eks. bekreftelse)
